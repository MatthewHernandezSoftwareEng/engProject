<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.EngProject" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/emerald_green</item>
        <item name="colorPrimaryDark">@color/emerald_green</item>
        <item name="colorAccent">@color/emerald_green</item>
        <item name="android:windowBackground">@color/background_color</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">@font/pixelify_sans_bold</item>
    </style>
</resources>
